server:
  port: 8081

spring:
  security:
    user:
      name: ansh
      password: ansh

  datasource:
    url: jdbc:mysql:///batch_cmd
    password: root1234
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true